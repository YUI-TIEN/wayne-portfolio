// Injected by vite.config.ts `define` at build time: the date of the last
// commit that touched any home-page copy file. Used for the visible "Updated"
// stamp in the Now block, so freshness comes from git rather than from
// somebody remembering to edit a hardcoded month.
declare const __CONTENT_UPDATED__: string
